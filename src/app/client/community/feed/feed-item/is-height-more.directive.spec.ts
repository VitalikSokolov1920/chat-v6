import { IsHeightMoreDirective } from './is-height-more.directive';

describe('IsHeightMoreDirective', () => {
  it('should create an instance', () => {
    const directive = new IsHeightMoreDirective();
    expect(directive).toBeTruthy();
  });
});
